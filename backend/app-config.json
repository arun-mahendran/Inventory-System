{
  "stack": "python_3_11",
  "build_path": ".",
  "command": "sh -c 'python3 -m pip install --user -r requirements-min.txt && echo STARTING_UVICORN && python3 -m uvicorn app.main:app --host 0.0.0.0 --port $X_ZOHO_CATALYST_LISTEN_PORT --log-level debug; echo UVICORN_EXIT_CODE:$?'",
  "memory": 1024,
  "env_variables": {},
  "scripts": {}
}